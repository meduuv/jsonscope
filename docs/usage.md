# Usage

`jsonscope` accepts JSON from files or standard input. Validation, formatting, and tree inspection are designed for local developer workflows.

Examples:

```text
jsonscope data.json
cat data.json | jsonscope --pretty
```
